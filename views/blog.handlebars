{{!-- Displays the information of the selected blog post --}}
<div class="text-center">
  <h2>{{title}}</h2>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <span class="dollar-amount">{{content}}</span>
  </p>
  <p>
    <button class="btn btn-primary">Fund this Project!</button>
  </p>
</div>
{{!-- Post title, content, post creator's username, date created --}}
{{!-- Allows the addition of comments --}}